#!/bin/sh
# Push-time gate. Runs the SAME checks, in the same order, with the same stub
# env as the CI "test" job (ci.yml) — so a push that passes here cannot make CI
# go red. See scripts/gates-push.mjs for the exact step list.
node scripts/gates-push.mjs